#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: integer parameter
 * Return: Always Success
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
