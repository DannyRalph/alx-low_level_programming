#include <stdio.h>
#include <stdlib.h>
/**
 * main -entry point
 * Return: Always 0 (Success/correct)
 */
int main(void)
{

	int n;

	while (n < 10)
	{

		putchar(n);
		n++;
	}

	putchar('\n');

	return (0);
}
